<div dir="rtl" style="text-align: right;">

# AI-usage

תיעוד השימוש ב-AI לאורך המבחן — מ-Architecture (חלק ב'/ג') דרך המימוש (חלק א') ועד הבדיקות. השימוש היה נרחב ומכוון: ה-AI שימש כפרטנר תכנון ו-Reviewer, לא ככלי "כתוב לי קוד" חד-פעמי.

## 1. באילו כלי AI השתמשתי

**Claude Code** (Claude, מודל Sonnet 5), בשיחה מתמשכת לאורך כל המבחן — לא כלי השלמת-קוד נקודתי (כמו Copilot) אלא סוכן שקרא את הריפו, כתב מסמכים וקוד, הריץ פקודות (`dotnet build`/`test`, `sqlite3`, `curl`), וענה כ-Reviewer בתפקיד נפרד לפני המימוש.

## 2. באילו שלבים

| שלב | שימוש |
|---|---|
| **Architecture (חלק ב'/ג')** | תכנון ראשוני של המבנה, זרימת ה-Outbox האסינכרונית להתראות, ותכנון הפריסה ב-Cloud — לפני כתיבת קוד |
| **Planning / Spec** | `requirements.md` (WHAT, EARS phrasing), `design-feature.md` (HOW, ADRs) — נכתבו יחד עם ה-AI, כולל טבלת Coverage שמקשרת דרישה↔החלטה↔קוד |
| **סקירה עצמאית (Review)** | לפני המימוש, הרצתי את ה-AI בתפקיד **Reviewer בכיר** על שני המסמכים מול ה-Brief ומול קוד המקור הקיים. הפלט: `spec-review-fixes.md` — 10 תיקונים ממוספרים, כל אחד עם בעיה מדויקת וסיבה. **8 מתוך 10 יושמו כלשונם, 2 נדחו** (סעיף 4 למטה) |
| **מימוש (Backend + Frontend)** | כתיבת קוד לפי `tasks.md`, משימה-משימה, עם Gate של Build/Test בין המשימות |
| **בדיקות** | כתיבת 8 הטסטים (6 Repository/Validation + 2 Authentication), ובחירת מקרי הבדיקה עצמם (ר' סעיף 5) |
| **דיוני Stakeholder באמצע העבודה** | כשדרישת האימות התהפכה (`[STAKEHOLDER #1]` — הוספת Auth מלא) עברתי איתו מחדש על ADR-002 (במקור: identity מ-Header) ובניתי ADR-008 (JWT) כהחלטה נפרדת, מתועדת כ"reversal", לא כתיקון שקט |

## 3. אם השתמשתי ב-AI לתכנון — מה שיניתי מההצעה שקיבלתי ומדוע

הצעת התכנון הראשונית (מסמך שנשמר ב-`archive/eager-growing-matsumoto.md` כתיעוד תהליך, לא כמקור אמת) הייתה מנופחת ומעורבת (WHAT+HOW+WHEN בקובץ אחד). פיצלתי אותה בעצמי ל-`requirements.md`/`design-feature.md`/`tasks.md`, ואז הרצתי סבב **Review** נפרד על התוצר. השינויים המהותיים שיצאו מה-Review ויושמו:

| # | מה היה בהצעה המקורית | מה שונה | למה |
|---|---|---|---|
| 1 | דרישה ל-case-insensitive matching + `EF.Functions.Like` | הדרישה הוסרה; המימוש עבר ל-`string.Contains` | ה-case-insensitivity הושג "בטעות" בזכות SQLite בלבד — היה נשבר בשקט על PostgreSQL/SQL Server. `Contains` גם חוסך escaping ל-`%`/`_` שה-Brief לא ביקש |
| 2 | לא הוזכר שינוי Kind בקריאה מ-SQLite | נוסף Value Converter שמשחזר `DateTimeKind.Utc` בקריאה | SQLite מאבד את ה-Kind; בלעדיו ה-JSON מאבד את ה-`Z` והדפדפן מציג זמן מקומי — כשל שקט לגמרי, החיפוש עצמו ממשיך לעבוד |
| 3 | `status=99` (ערך enum לא מוגדר) — לא טופל | נוספה בדיקת `Enum.IsDefined` ב-Validation | ה-Binder של ASP.NET Core מקבל כל מספר שלם ל-enum בלי לבדוק שהוא מוגדר; `status=99` היה מחזיר `200` עם רשימה ריקה במקום `400` — בדיוק הבליעה השקטה ש-`REQ-F-007` אוסר |
| 4 | 5 טסטים כללו "סינון מולטי-סטטוס" רגיל | הוחלף בטסט "התאמה באמצע-מחרוזת" (ולבסוף שולב מולטי-סטטוס עם הרשאות יחד) | `IN (...)` כמעט לא יכול להיכשל בשקט; `StartsWith` שנכתב מהרגל דווקא יכול — וזה בדיוק המלכודת שתועדה ב-ADR-006 |
| 5 | סדר בנייה שלא ציין שהצעד השלישי שובר את ה-build | נוסף איזכור מפורש שהטסטים הישנים נמחקים **באותו צעד** | בלי זה, מי שמריץ את התוכנית פוגש build אדום באמצע בלי דרך להבדיל בין כישלון צפוי לטעות שלו |
| 6 | לא צוין שה-seed guard וה-seed dates קריטיים אחרי מעבר ל-SQLite קבוע | נוספו שני כללים מפורשים (guard ותאריכים יחסיים) | עם InMemory זה לא היה משנה כלום (הכל מתאפס בכל הרצה); עם קובץ מתמיד, בלי guard כל הפעלה שנייה מכפילה 200,000 שורות |
| 7 | לא הוגדר מה קורה בעמוד מעבר לאחרון | נוספה דרישה מפורשת: `200` עם `items: []` וה-`totalCount` הנכון, לא שגיאה | בלי הגדרה, כל מי שכותב קוד בוחר בשקט — ואי-אפשר לכתוב טסט נגד "לא מוגדר" |
| 8 | ADR-007 בחר אופציה C — קובץ `site.config.json` הנקרא ב-Runtime | הוחלף באופציה B — `environment.ts` (Build-time) | שום דבר ב-Brief לא ביקש קונפיגורציה שניתנת לעריכה אחרי ה-Build; C היה עולה Application Initializer, טיפול בכשל טעינה, ונכס סטטי שצריך להוציא מטיפול Build-output — עלות אמיתית מול תקציב של 3 שעות (**נהפך בחזרה מאוחר יותר** — ר' סעיף 4) |
| 9 | "Authentication" הופיע פעמיים במשמעויות שונות (Out of Scope מול ADR-002) | נוספה פסקת "Terminology" בשני המסמכים | ADR-002 בחר "authentication scheme" של ASP.NET Core (ה-pipeline: Handler, `[Authorize]`, 401), לא אימות Credential — בלי הבהרה זו נקודה קלאסית להיתפס עליה בראיון |
| 10 | טבלת ה-Coverage לא הייתה מסונכרנת עם §3.4a/§6 שנוספו אחרי | עודכנה טבלת ה-Coverage כדי שתשקף את המסמך המעודכן | טבלה שאינה מסונכרנת מפסיקה להיות הוכחה שאין דרישה יתומה והופכת לקישוט |

**שינוי גדול נוסף, לא מה-Review אלא מדרישת Stakeholder באמצע העבודה:** ADR-002 (זהות מגיעה מ-Header, ללא Auth אמיתי) **לא נמחק** אלא סומן כ-Superseded, ונוסף ADR-008 (JWT מלא). הסיבה לתעד ולא לדרוס: ADR-002 עצמו ניבא (ב"Consequences") שהמעבר לטוקן אמיתי ישנה רק את רישום ה-Scheme וש-`ClaimsCurrentUserAccessor` יישאר בלי שינוי — התחזית הזו **נבדקה בפועל** ב-ADR-008 ואומתה: אכן שום שינוי לא נדרש ב-`ClaimsCurrentUserAccessor`.

## 4. האם היו הצעות שדחיתי — דוגמאות והסבר

| הצעה שנדחתה | מקור | למה נדחתה |
|---|---|---|
| **ADR-007 אופציה C** — `site.config.json` הנקרא ב-Runtime, כברירת המחדל הראשונית | הצעת ה-AI המקורית (הועדפה על B "כי בקשו קונפיגורציה ב-Runtime") | בבדיקה חוזרת מול ה-Brief לא נמצאה דרישה כזו בפועל. אופציה B (build-time, `environment.ts`) משיגה את התכונה החשובה — "אין URL מפוזר בקוד" — כמעט בחינם, מול עלות אמיתית (Initializer, טיפול כשל טעינה, נכס שצריך החרגה) שלא הוצדקה. הוחלפה חזרה כאשר Stakeholder אכן דרש Runtime config — ותועד כ"retained alternative", לא כטעות |
| **`EF.Functions.Like` להתאמה חלקית** | הצעת AI ראשונית ל-ADR-006, ל"שמירה על case-insensitivity" | ה-case-insensitivity עצמה נדחתה (ר' טבלה לעיל) — ומשנדחתה, ל-`LIKE` לא נשאר יתרון, רק עלות: `%`/`_` בקלט משתמש הופכים ל-Wildcards ודורשים Escaping. `Contains` (מתורגם ל-`instr`) חף מהסיכון הזה לגמרי |
| **אינדקסים חד-עמודתיים על `Status`/`RequestType`** | הצעת AI ראשונית ("להוסיף אינדקסים לכל עמודת סינון") | **נמדד ולא הונח**: עם האינדקסים האלה קיימים, ה-Planner מעדיף אותם על פני אינדקסי ההרשאה ברגע שיש פילטר Status — והחיפוש המסונן עלה מ-0.4ms ל-**41ms**. אינדקס שה-Planner בוחר בטעות גרוע מאינדקס שלא קיים בכלל |
| **`StartsWith` להתאמה חלקית** (במקום Contains) | נשקל בתהליך התכנון כאופציה "בטוחה יותר" (sargable) | ה-Brief מגדיר "התאמה חלקית" כהתאמה **בכל מקום** במחרוזת, לא רק בתחילתה; כל מספרי הבקשה חולקים prefix קבוע (`REQ-`), אז Prefix search היה מכריח את המשתמש להקליד prefix שלא נושא מידע — מנוגד ישירות למשמעות "חלקי" |
| **Keyset/Seek pagination** במקום Offset | נשקל כחלופה ל-ADR-003 | `REQ-N-002` דורש `totalCount` לצורך ה-Paginator; Keyset לא נותן ספירה כוללת ולא מאפשר קפיצה לעמוד שרירותי — לא עומד בדרישה כפי שהיא כתובה. תועד כ"הצעד הבא" אם דפוס הגלילה ישתנה |
| **ASP.NET Core Identity מלא** (`IdentityDbContext`/`UserManager`) | חלופה A ב-ADR-009 | מביא סט טבלאות (`AspNetUsers` וכו') ומשטח יכולות (Lockout, Email confirmation, External login) שדבר במבחן לא ביקש, מול מודל של שני תפקידים וחשבונות מזרועים. `PasswordHasher<User>` העירום נותן את אותה הצפנת PBKDF2 בלי המשטח |
| **RS256** (חתימה א-סימטרית) ל-JWT | נשקל לאור ADR-001 (`REQ-A-001`, פיצול עתידי ל-Microservices) | אף שירות נוסף לא מאמת טוקן היום — המכונה הנוספת (ניהול זוג מפתחות, JWKS endpoint) לא קונה כלום כרגע. HS256 נבחר, ו-RS256 תועד כ"הצעד הבא" ברגע שיהיה שירות שני שצריך לאמת עצמאית |
| **Mediator pattern** לזרימת ה-Login | נשקל כדי "להתאים" למוסכמה נפוצה | שום דבר בפרויקט לא כבר משתמש ב-Mediator; להכניס אחד עבור זרימת Login בת 3 צעדים זה בדיוק ה-Premature Abstraction ש-§1 עקרון 1 אוסר. נבחר `AuthService` פשוט, באותה צורה בדיוק כמו `RequestService` הקיים |
| **קומפוננטת Angular נפרדת לטופס הסינון** | נשקל לצורך "הפרדת אחריות" קלאסית | הטופס לא ישמש בשום מקום אחר; הפיצול היה עולה קריאוּת (העברת FormGroup דרך גבול קומפוננטה) בלי לקנות כלום. הפיצול שכן שווה — טבלת התוצאות — בוצע, כי שם יש גבול אמיתי (למנוע מיון/עימוד בצד לקוח) |

## 5. כיצד וידאתי שהתוצר נכון

וידוא נעשה **במדידה בפועל**, לא בהנחה — עקרון שחזר לאורך כל העבודה:

| מה נבדק | איך |
|---|---|
| ביצועי האינדקסים | `EXPLAIN QUERY PLAN` אמיתי מול `requests.db` הנזרע — הפלט (`MULTI-INDEX OR` על שני האינדקסים המורכבים) הועתק ותועד ב-README, לא תואר "בערך" |
| ההשפעה של אינדקס Status/RequestType | נמדד בפועל: 0.4ms בלעדיו מול 41ms איתו, על אותם 200,000 רשומות |
| זמן ה-Seed | נמדד: כ-7 שניות ל-200,000 שורות דרך CTE בודד, מול "דקות" ב-`AddRange` שורה-שורה (לא הורץ במלואו — הוסק מהמנגנון) |
| אכיפת ה-FK בפועל | `INSERT` ידני עם `OwnerId=999999` הורץ ישירות — נדחה עם `FOREIGN KEY constraint failed` כשה-Pragma דלוק (ברירת המחדל של EF Core), עבר כשכבוי (ה-CLI הגולמי) — הוכחה שהאכיפה אמיתית, לא רק מוצהרת בסכימה |
| ספירות הרשאה (372 מול 200,000) | נמדדו ישירות מול ה-DB (`SELECT COUNT(*) WHERE OwnerId=... OR AssignedToUserId=...`), לא חושבו תיאורטית — כולל תיקון עצמי: זוהה ש-372 עבור משתמש 1 חוזר על עצמו מהנוסחה הקיימת, אבל 372 עבור משתמש 2 הוא ערך **חדש** שלא נמדד קודם ולא הונח שווה |
| Build/Test בכל שלב | `dotnet build`/`dotnet test` הורצו כ-Gate בין כל שתי משימות ב-`tasks.md` — משימה לא סומנה "בוצע" בלי שה-Gate שלה עבר בפועל; הטסטים הורצו **פעמיים ברצף** לוודא שאין דליפת state בין טסטים (SQLite in-memory משותף) |
| ה-API מקצה לקצה | סדרת `curl` אמיתית מול שרת רץ — Login נכון/שגוי, בקשה בלי Token/עם Token שפג/עם Token תקין, ערכי enum לא חוקיים, `page` מעבר לאחרון — לא רק קריאה של הקוד |
| זרימת Login מלאה בדפדפן | נבדק ידנית: כניסה עם שני החשבונות הנזרעים, סיסמה שגויה (נשאר על העמוד + הודעה כללית), Logout (חוזר ל-`/login`, ה-Guard חוסם גישה חוזרת) |
| עקביות המפרט מול הקוד | טבלת Coverage ב-`requirements.md` נבדקה בשתי הכיוונים מול §6 של `design-feature.md` בסוף כל סבב שינויים — לא הונחה שהיא עדיין נכונה |
| Review עצמאי לפני מימוש | ה-AI הורץ בתפקיד סוקר בכיר על המפרט **לפני** כתיבת קוד, וההמלצות שלו עברו סינון (8/10 יושמו, 2/10 נדחו במפורש — סעיף 4) ולא יושמו אוטומטית |

</div>
